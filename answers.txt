1.gitignore file has the list of file which we can ignore them not to add to the repository

2.If the master is not diverged from the new branch created and instead of commiting if we can just move the pointer to the latest commit then it is called fast forwarding

3.By having different branches we can have multiple persons working on the same project and when they are done they can just push it to the master branch, by this we can avoid conflicts of multiple copies of the same file

4.We can have as many repositories as we need but we will have one default remote repository created by github which is origin 